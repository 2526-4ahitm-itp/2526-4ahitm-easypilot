# EasyPilot companion backend configuration

# Application metadata (exposed via /api/status)
easypilot.version=1.0-SNAPSHOT

# Logging — JBoss LogManager via Quarkus
quarkus.log.level=INFO
quarkus.log.console.format=%d{HH:mm:ss.SSS} %-5p [%c{2.}] (%t) %s%e%n
quarkus.log.category."com.example".level=DEBUG
