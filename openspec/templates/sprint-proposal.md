# Proposal: Sprint NN - <Short Name>

> Copy this file to `openspec/changes/<YYYY-MM-DD-sprint-NN-name>/proposal.md`
> and replace every `<…>` placeholder. Delete this blockquote when done.

## Context

<1–3 sentences on why this sprint exists. What is the current state, what
is missing, and what triggered the sprint (user request, demo deadline,
follow-up from previous sprint, …)?>

## Goal

As a <role>, I want to <capability> so that <outcome>.

## In Scope

- <Bullet list of concrete deliverables. Be specific — "add foo" beats
  "improve foo".>
- <…>

## Out of Scope

- <Bullet list of things explicitly *not* in this sprint, even if they
  feel related. Saves arguments later.>

## Affected Specs

| Spec | Change type | Notes |
|---|---|---|
| `<capability>` | ADDED / MODIFIED / REMOVED | <1 line> |

If a new capability is introduced, list it here and create
`changes/<sprint>/specs/<new-capability>/spec.md`.

## Acceptance Criteria

- [ ] <Testable bullet 1>
- [ ] <Testable bullet 2>
- [ ] <…>

## Risks / Open Questions

- <Anything that might block the sprint or change scope mid-way.>

## Out-of-Band Coordination Required

- <Anyone you need to talk to before merging — handler, hardware owner,
  test pilot, …>

## Done When

- All acceptance criteria are ticked
- Delta specs are folded into `openspec/specs/`
- Sprint folder is moved to `openspec/changes/archive/`
- Code is committed (do **not** push without handler approval, per
  `guidelines.md`)
